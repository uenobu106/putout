.container
  %h1 新着ニュース
  = link_to 'ニュースを追加する',new_post_path
  %hr
  - @post.each do |post|
    %p= link_to post.title, "/posts/#{post.id}"
    -# %p= post.content
    =link_to '編集', edit_post_path(post.id)
    =link_to '削除', post_path(post.id),method: :delete, data: { confirm: '本当に削除していいですか？'}
    %hr
